INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/include 
					${OSG_INCLUDE_DIRS} 
					${OSGDB_INCLUDE_DIR} 
					${OPENTHREADS_INCLUDE_DIR})

SET(TARGET_LIBRARIES_VARS  	OPENTHREADS_LIBRARY
							OSG_LIBRARY 
							OSGDB_LIBRARY 
							OSGUTIL_LIBRARY 
							OSGGA_LIBRARY 
							OSGVIEWER_LIBRARY )

SET(TARGET_HEADERS Utils.h DataTexture.h ComputeNode.h GPUParticles.h )
SET(TARGET_SRC main.cpp )

SET( TARGET_COMMON_LIBRARIES osgSprites)

SET( TARGET_LIBS
    ${OPENGL_gl_LIBRARY}  
)

SETUP_APPLICATION(GPUParticles)
